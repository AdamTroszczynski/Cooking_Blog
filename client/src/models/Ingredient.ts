export default class Ingredient {
  id: number;
  name: string;
  qua: string;

  constructor(id: number, name: string, qua: string) {
    this.id = id;
    this.name = name;
    this.qua = qua;
  }
}
